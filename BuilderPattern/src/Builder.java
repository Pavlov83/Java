public class Builder {

    /*Sometimes we need to create new object from sub-objects
    A composite or aggregate object is what is created

    Builder pattern hides the process of building complex objects
    and separates the representation and construction of an object
    This allows us to construct different classes with the same process

     */

}
