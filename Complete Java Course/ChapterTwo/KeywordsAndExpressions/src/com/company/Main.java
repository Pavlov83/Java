package com.company;

public class Main {

    public static void main(String[] args) {

        int int2 = 5;
        //e.g. false,true,null are few of the 50 reserved keywords

        //Expressions are building blocks in Java(consists of
        //Variables,Values and operators(if we add data type this
        //becomes a valid expression
        double kilometers = (100 * 1.609344);

        //Expression is also the following example:

        int highScore = 50;

        if(highScore == 50){
            System.out.println("This is an expression");

        }

    }
}
